

<%= form_for @user do |f| %>
  Username: <%= f.text_field :username, placeholder: "#{@user.username}" %><br/>
  Email: <%= f.text_field(:email, placeholder: '#{@user.email}')%><br/>
  Password: <%= f.password_field :password %><br/>
  <%= f.submit 'Submit' %>
<% end %>
